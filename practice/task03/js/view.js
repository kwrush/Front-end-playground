(function(window) {
	'use strict';

	function CategoryListView(template) {
		this.template = template;
		this.listWrapper = qs('.app-list[data-list-level="1"] > li');
		this.listTitleIcon = qs('h2 > i.fa', list);
		this.list = qs('.app-list[data-list-level]')
	};

	CategoryListView.prototype.bind = function() {}；
}(window));